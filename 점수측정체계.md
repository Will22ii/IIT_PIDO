# AION 점수 측정 체계

> 작성일: 2026-08-30
> 근거: 구글 드라이브 과제 문서(중간보고·주간보고) + `scaling` 브랜치 코드(`2edbc7e`) + 벤치마크 함수 수치 검증
> 목적: 국책과제 KPI와 코드가 실제로 계산하는 점수를 하나로 대응시켜 정리한다.

---

## 1. 세 층으로 되어 있다

점수는 성격이 다른 세 층이 겹쳐 있고, 구분하지 않으면 숫자가 서로 안 맞는 것처럼 보인다.

| 층 | 무엇 | 판정 단위 | 기준 |
| --- | --- | --- | --- |
| **① 국책 KPI** | 과제 최종 개발목표. 대외 보고용 | 벤치마크별 **run 평균** / task별 **성공률** | PASS / FAIL |
| **② task 점수** | FS · DSE · opt 각각의 성공률 | 110 runs 집계 | ≥ 95 |
| **③ run 단위 판정** | run 하나가 통과했는지 (0/1) | run | true / false |

---

## 2. Task는 세 개다 — FS, DSE, opt

| task | 모듈 | 하는 일 | 점수 컬럼 |
| --- | --- | --- | --- |
| **FS** | Modeler | 중요 변수 선별 (Feature Selection) | `fi_real_only_success_pct` |
| **DSE** | Explorer | 유효 설계공간 탐색 (Design Space Exploration) | `explorer_bounds_pass_pct` |
| **opt** | Optimizer | 최종 최적해 도출 | `optimizer_goal_hit_pct` |

---

## 3. ★ 핵심 — task 점수는 서로 독립이다

**이게 이 문서에서 가장 중요한 부분이다.** 앞 task가 실수해도 뒤 task 점수를 깎지 않는다.

| 상황 | 판정 |
| --- | --- |
| FS가 **dummy 변수를 잘못 채택**했는데, DSE가 영역을 잘 잡았다 | **DSE 점수 안 깎는다.** DSE 성공 |
| FS 또는 DSE가 실패했는데, opt가 목표 점수를 맞췄다 | **opt 점수 안 깎는다.** opt 성공 |
| DSE가 영역을 잘못 잡았는데, opt가 목표를 넘겼다 | **opt 성공 처리** |
| **FS에서 중요 변수(real variable)가 아예 탈락**했다 | **DSE 무조건 실패** |

마지막 줄만 예외다. 그리고 이건 별도 규칙을 붙인 게 아니라 **구조상 자동으로 그렇게 된다.**

### 왜 자동인가

`selected_bounds`는 FS가 고른 변수에 대해서만 만들어진다. 중요 변수가 탈락하면 그 차원의 경계 자체가 존재하지 않는다. `_is_optimum_included()`는 strict mode로 동작한다:

```python
# pipeline/run_pipelines.py L801
# strict mode: known optimum의 key가 selected_bounds에 하나라도 없으면 미포함 처리
if any(k not in selected_bounds for k in required_keys):
    continue   # → 미포함
```

known optimum의 키 중 하나라도 `selected_bounds`에 없으면 곧바로 미포함 → `survivor_optimum_included = false` → `explorer_bounds_pass = false`.

반대로 **dummy가 더 끼어드는 건 무해하다.** 검사는 known optimum의 키만 순회하므로, `selected_bounds`에 여분의 키가 있어도 판정에 영향이 없다. 그래서 "FS가 dummy를 채택해도 DSE는 안 깎인다"가 성립한다.

### 곱셈 컬럼은 진단용이다

코드에는 곱셈 컬럼도 있다.

```
explorer_joint_pass  = modeler_all_real_only × explorer_bounds_pass
optimizer_final_pass = explorer_joint_pass  × optimizer_goal_hit
```

이건 **파이프라인 전체가 한 번에 끝까지 통과한 비율**을 보는 진단 지표다. 병목이 어느 task인지 분해할 때 쓴다. **task별 KPI 점수는 이 곱셈이 아니라 위 표의 독립 컬럼으로 매긴다.** 둘을 섞지 않는다.

---

## 4. 국책 KPI — 최종 개발목표

중간보고(2026-07-08) `[첨부] 최적화 Algorithm (AION) 성능 평가` 기준. **HEEDS 동등 이상 수준 확보**가 목표다.

| 주요 성능지표 | 최종 개발목표 (국책 KPI) | I.PIDO 결과 | 판정 | HEEDS 결과 |
| --- | ---: | ---: | :-: | ---: |
| Feature Importance 성공률 | 95 이상 | 100 | PASS | – |
| 유효 설계공간 탐색 성공률 | 95 이상 | 96 | PASS | – |
| 벤치마크함수 1 (Goldstein-Price) | 3.60 이하 | 3.1 | PASS | 3.0 |
| 벤치마크함수 2 (Rosenbrock) | 1.81 이하 | 1.12 | PASS | 1.0 |
| 벤치마크함수 3 (Cantilever Beam) | 114.66 이하 | 93.01 | PASS | 95.55 |
| 벤치마크함수 4 (Six-Hump Camel Back) | -0.86 이하 | -1.03 | PASS | -1.03 |

앞의 두 지표가 **"95점"의 출처**다. 성공률이므로 단위는 %다. 뒤의 네 개는 목적함수 값이라 단위가 다르다 — **같은 표에 성격이 다른 두 종류가 섞여 있다.**

> Rosenbrock만 KPI 표기(1.81)와 코드 값(1.20)이 다르다. **코드 값이 기준이다.** 근거는 §5 참조.

### 4.1 벤치마크 4종은 run 평균으로 판정한다

벤치마크 KPI는 성공률이 아니다. **해당 벤치마크의 run 수만큼 도달한 최적점의 평균이 최종 개발목표를 만족하면 PASS다.**

| 벤치마크 | run 수 | 판정식 |
| --- | ---: | --- |
| Goldstein-Price | 50 | 50개 run의 best objective **평균** ≤ 3.60 |
| Rosenbrock | 10 | 10개 run의 best objective **평균** ≤ 1.20 |
| Cantilever Beam | 25 | 25개 run의 best objective **평균** ≤ 114.66 |
| Six-Hump Camel | 25 | 25개 run의 best objective **평균** ≤ -0.86 |

run 수가 벤치마크마다 다르므로(50 / 25 / 25 / 10) 벤치마크 간에 평균을 다시 평균 내면 안 된다. 위 표의 I.PIDO 결과(3.1 / 1.12 / 93.01 / -1.03)가 그 평균값이다.

반면 **FS·DSE 두 지표는 run 평균이 아니라 성공률(pass 비율)**이다. 판정 방식이 다르다.

---

## 5. ★ 목표값이 어디서 왔는지 — 검증 결과

기존 문서(`docs/goals/optimizer_experiment_goals.md`)는 "known optimum 대비 상대 gap"으로 설명하지만, **벤치마크마다 기준점이 다르다.** 특히 Cantilever Beam은 20%가 아니다.

### 5.1 Cantilever Beam 전역 최적해 검증

목적함수는 무게다.

```
V = (2·h1·b1 + (H − 2·h1)·b2) × 36
제약: sigma_max ≤ 5000,  delta_max ≤ 0.10
```

문서상 known optimum `H=7.0, h1=0.1, b1=9.48482, b2=0.1`에서:

| 항목 | 값 | 판정 |
| --- | ---: | --- |
| objective | **92.7707** | — |
| sigma_max | 4999.91 / 5000 | 여유 +0.09 — **binding** |
| delta_max | 0.0617 / 0.10 | 여유 있음 |

SLSQP 400회 multi-start로 제약 최적해를 다시 찾은 결과:

| 항목 | 값 |
| --- | ---: |
| `H=7.000000, h1=0.100000, b1=9.484626, b2=0.100000` | |
| objective | **92.7693** |
| sigma_max | 5000.0000 (정확히 경계) |

문서값과 차이 −0.0014. **known optimum 92.77은 맞다.** 응력 제약이 binding인 진짜 제약 최적해다.

### 5.2 목표값 — 코드 `score_goal`이 기준이다

**목표값의 정본은 `pipeline/run_pipelines.py`의 `score_goal`이다.** 문서·보고서 표기와 다르면 코드를 따른다.

| 벤치마크 | 전역 최적해 | **목표 (`score_goal`)** | 최적해 대비 gap |
| --- | ---: | ---: | ---: |
| Goldstein-Price | 3.0000 | 3.60 | 20.00% |
| Rosenbrock (shifted) | 1.0000 | 1.20 | 20.00% |
| **Cantilever Beam** | **92.77** | **114.66** | **23.60%** |
| Six-Hump Camel | -1.0316 | -0.86 | 20.00% (\|값\| ÷ 1.2) |

**Cantilever Beam은 20%가 아니라 23.60%다.** 이 문제는 응력·처짐 **제약이 걸려 있어** 다른 3종과 같은 배율을 쓰지 않는다. 목표는 114.66이다.

참고로 `114.66 = 95.55 (HEEDS 결과) × 1.2`로 정확히 떨어진다. 즉 CB는 무제약 최적해가 아니라 **제약 하에서 경쟁제품이 실제로 도달한 수준을 기준점**으로 잡은 값이다.

### 5.3 코드 값과 다른 표기 하나 — Rosenbrock

| 벤치마크 | 코드 `score_goal` | 중간보고 KPI 표기 | 기준 |
| --- | ---: | ---: | --- |
| Goldstein-Price | 3.60 | 3.60 | 일치 |
| **Rosenbrock** | **1.20** | 1.81 | **코드(1.20)를 따른다** |
| Cantilever Beam | 114.66 | 114.66 | 일치 |
| Six-Hump Camel | -0.86 | -0.86 | 일치 |

Rosenbrock의 1.81은 optimum×1.2(=1.2)로도 HEEDS×1.2(=1.2)로도 나오지 않는 값이다. 코드의 1.20이 shifted optimum 1.0 대비 20%로 규칙에 맞으므로 **코드 값을 기준으로 쓴다.**

### 5.4 목표 대비 중간보고 결과

| 벤치마크 | 전역 최적해 | 결과 | 최적해 대비 | 목표 | 여유 | 판정 |
| --- | ---: | ---: | ---: | ---: | ---: | :-: |
| Goldstein-Price | 3.0000 | 3.10 | +3.33% | 3.60 | 0.50 | PASS |
| Rosenbrock | 1.0000 | 1.12 | +12.00% | 1.20 | 0.08 | PASS |
| Cantilever Beam | 92.77 | 93.01 | **+0.26%** | 114.66 | 21.65 | PASS |
| Six-Hump Camel | -1.0316 | -1.03 | +0.16% | -0.86 | 0.17 | PASS |

Cantilever Beam은 제약 최적해에서 0.26% 이내로 들어왔고 HEEDS(95.55)보다도 낫다.

**가장 빠듯한 건 Rosenbrock이다** — 1.12 대 목표 1.20, 여유 0.08뿐이다. KPI 표의 1.81 때문에 여유 0.69로 보였지만 실제 기준은 1.20이다.

---

## 6. Task별 지표 정의

### 6.1 FS (Modeler)

| 항목 | 내용 |
| --- | --- |
| 지표명 | `fi_real_only_success_pct` |
| run 단위 | `modeler_all_real_only` |
| 정의 | `selected_features == real_variables` — 선택 변수 집합이 실제 변수 집합과 **정확히** 일치 |
| 목표 | ≥ 95 |
| 보조 지표 | `fi_all_real_included_pct` — 실변수 전부 포함 여부, dummy 혼입은 허용 |

`fi_all_real_included`는 "실변수를 다 넣었나"만 보고, `fi_real_only_success`는 "dummy를 하나도 안 넣었나"까지 본다. **FS 점수는 후자(strict)로 매긴다.** 다만 **DSE 판정을 강제 실패시키는 조건은 전자다** — 실변수가 탈락했을 때만이고, dummy 혼입은 DSE에 영향 없다 (§3).

`real_variables`는 해당 ProblemCase의 `known_optimum` 키에서 도출한다.

### 6.2 DSE (Explorer)

| 항목 | 내용 |
| --- | --- |
| 지표명 | `explorer_bounds_pass_pct` |
| 정의 | `survivor_optimum_included` ∧ `volume_cap_pass` |
| volume cap | `volume_ratio ≤ 0.25` |
| 목표 | ≥ 95 |

두 조건을 **동시에** 만족해야 한다. 경계 안에 optimum이 들어와도 탐색공간이 원래의 25%를 넘으면 실패고, 너무 좁혀서 optimum을 잘라내도 실패다.

| 실패 유형 | 조건 | 의미 |
| --- | --- | --- |
| `over_shrink_fail` | optimum 미포함 ∧ volume 통과 | 과하게 좁혀 정답을 잘라냄 |
| `over_wide_fail` | optimum 포함 ∧ volume 초과 | 정답은 품었지만 공간이 넓음 |
| `both_fail` | 둘 다 실패 | — |

### 6.3 opt (Optimizer)

| 항목 | 내용 |
| --- | --- |
| 지표명 | `optimizer_goal_hit_pct` |
| 알고리즘 | `focus_bo` (Focus0 → Focus1 → Focus2 → Focus3) |
| 판정 | minimization이면 `best_objective ≤ goal`, maximization이면 `≥ goal` |
| goal 출처 | ProblemCase의 `score_goal`, 없으면 `optimizer_goal` |

`optimizer_goal = None`이면 조기 종료 없이 예산을 끝까지 쓰고, summary pass 판정에는 `score_goal`만 쓴다. goal 달성 후 best objective가 5회 연속 개선되지 않으면 조기 종료할 수 있다 — 예산 삭감이 아니라 이미 목표를 달성한 run의 낭비를 줄이는 정책이다.

**DSE가 영역을 잘못 잡았어도 opt가 goal을 넘기면 opt는 성공이다** (§3).

---

## 7. Benchmark 상세

현재 활성 문제(`ACTIVE_PROBLEM_CASES`)와 고정 예산이다. 모두 minimization 기준.

| Benchmark | 전역 최적해 | 목표 `score_goal` | gap | DOE n_samples | Opt n_samples | repeats |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Cantilever Beam | 92.77 | 114.66 | 23.60% | 90 | 60 | 25 |
| Rosenbrock (shifted) | 1.0000 | 1.20 | 20.00% | 450 | 1050 | 10 |
| Goldstein-Price | 3.0000 | 3.60 | 20.00% | 150 | 350 | 50 |
| Six-Hump Camel | -1.0316 | -0.86 | 20.00% | 35 | 15 | 25 |
| **합계** | | | | | | **110 runs** |

- **Rosenbrock**은 objective에 `+1` shift가 있다. 수학적 원래 optimum은 0.0이지만 benchmark objective optimum은 1.0이다.
- **Six-Hump Camel**은 known optimum이 두 점 `(0.0898, −0.7126)`, `(−0.0898, 0.7126)`이다. 둘 중 하나만 경계에 들어와도 포함으로 친다.
- **Cantilever Beam**은 응력 제약(`sigma_max ≤ 5000`)이 binding이다.
- **`*_nodummy` 변형 4종은 실험용이며 점수용이 아니다.** preset에 정의돼 있으나 `ACTIVE_PROBLEM_CASES`에서 비활성 상태이고, 점수 집계에 넣지 않는다.

### 문제별 관찰 포인트

| Benchmark | 성격 | 볼 것 |
| --- | --- | --- |
| Rosenbrock | narrow valley | near-hit 실패, best_local / local_probe / refine 품질 |
| Goldstein-Price | multi-modal | source mixture 균형, random/topk 균형 |
| Cantilever Beam | 제약 binding | 경계 근처 수렴 안정성 |
| Six-Hump Camel | 저차원 multi-modal | 빠른 goal hit 여부, 과탐색 여부 |

---

## 8. 집계 규칙

- **task 성공률은 110 runs micro 평균이다.** 벤치마크 동등가중(macro)이 아니다. repeats가 문제마다 다르므로(50 / 25 / 25 / 10) micro와 macro는 다른 값이 나온다.
- **벤치마크 목적함수 KPI는 해당 벤치마크 run들의 best objective 평균이다.** 벤치마크끼리 다시 평균 내지 않는다.
- `problem = __overall__` 행이 전체 평균이다.
- **서로 다른 mode의 결과를 같은 표에서 섞지 않는다.** mode는 `aion_mode`와 켜진 task 조합에서 자동 도출된다 (`_resolve_batch_mode`) — `aion` / `full` / `opt_only` / `<task조합>`.
- full pipeline 결과와 optimizer-only 결과를 같은 표에 놓지 않는다.
- **숫자 집계를 사람이나 LLM이 하지 않는다.** 배치 종료 시 `run_pipelines.py`가 만든 stats CSV 또는 스크립트가 한다.

---

## 9. 산출물 — stats CSV 6종

`result/bench/<mode>/<batch_ts>/stats/` 아래에 생성된다.

```
fi_primary_try_stats_*.csv            fi_primary_problem_summary_*.csv
explorer_strategy_try_stats_*.csv     explorer_strategy_problem_summary_*.csv
optimizer_try_stats_*.csv             optimizer_problem_summary_*.csv
```

**가장 먼저 볼 파일: `optimizer_problem_summary_*.csv`**

| 컬럼 | 의미 | 성격 |
| --- | --- | --- |
| `modeler_all_real_only_pct` | **FS 점수** | task 독립 |
| `explorer_bounds_pass_pct` | **DSE 점수** | task 독립 |
| `optimizer_goal_hit_pct` | **opt 점수** | task 독립 |
| `explorer_joint_pass_pct` | FS + DSE 동시 통과 비율 | 진단용 (곱) |
| `optimizer_final_pass_pct` | 파이프라인 전체 통과 비율 | 진단용 (곱) |

`try_stats`는 컬럼이 많다 — `explorer_strategy_try_stats`는 158컬럼이므로 통째로 읽지 말고 필요한 컬럼만 투영한다.

---

## 10. 진단용 실패 분류

task 점수와 별개로, 파이프라인 전체를 볼 때 쓰는 분류다.

```
feature_fail    : modeler_all_real_only = false
explorer_fail   : modeler_all_real_only = true,  explorer_bounds_pass = false
optimizer_fail  : explorer_joint_pass  = true,  optimizer_goal_hit   = false
pipeline_pass   : optimizer_final_pass = true
```

Focus3 고도화의 직접 대상은 `optimizer_fail`이다. `feature_fail` · `explorer_fail`은 기록하되 Focus3 성패 판단에 섞지 않는다.

---

## 11. 분석 순서

1. `optimizer_problem_summary_*.csv`에서 **task별 독립 점수 3개** 확인 — `modeler_all_real_only_pct`, `explorer_bounds_pass_pct`, `optimizer_goal_hit_pct`
2. 벤치마크별 best objective 평균이 KPI 목표를 만족하는지 확인
3. 곱셈 컬럼(`optimizer_final_pass_pct`)으로 병목 task 분해
4. Focus3 책임 구간(`explorer_joint_pass = true` ∧ `optimizer_goal_hit = false`) 특정
5. 해당 run의 OPT debug history와 metadata 열람
6. 실패 run의 source / acquisition / refine / recovery / dedup / budget 사용량을 **성공 run과 비교**

실패만 보면 원인 규명도 안정성 진단도 안 된다. 아슬아슬한 성공(goal 대비 마진이 얇은 run)과 매칭 성공 대조군을 함께 본다.

---

## 12. 점수 실험 시 지켜야 할 제약

1. **`n_samples`를 줄이지 않는다.** 실행 시간 단축 목적으로도 금지.
2. **`repeats`를 줄이지 않는다.**
3. Score run 중 Focus3 pool size, acquisition refinement, GP update policy 같은 후보 품질 knob를 조용히 낮추지 않는다.
4. `CAE_model/` benchmark 함수를 수정하지 않는다.
5. **known optimum은 평가 전용이다.** 탐색·분기 로직에서 정답 좌표를 참조하지 않는다.
6. **benchmark 이름 기반 분기 금지.** 모든 로직은 상태/특성 기반으로 일반화한다.
7. 사후 라벨(`joint_pass`, `fail_type`, optimum 포함 여부)을 실시간 입력으로 쓰지 않는다.

**허용**: goal 달성 후 patience 기반 조기 종료, timing/profiling 로그, 후보 선택이 바뀌지 않는 범위의 캐싱·벡터화·병렬화, 배치 러너 레벨 병렬화, debug plot을 task 종료 시점에 일괄 생성.

**명시적 검증 필요**: `gp_refit_every` 증가, Focus3 plan pool size 감소, `focus3_refine_starts` 감소, Focus2 local candidate pool size 변경, exact GP → approximate surrogate 대체.

---

## 13. 달성 이력

| 시점 | 내용 | 출처 |
| --- | --- | --- |
| 2026-02 | 신규 알고리즘 간이 평가(10회 반복) 결과 과제목표 만족 확인 | 주간보고 08주차 |
| 2026-05 | 설계공간 탐색 성공률 95% 목표 달성 위한 전략 고도화 | 주간보고 20주차 |
| 2026-06 | 최적화 성공률 **92점 → 96.2점** | 주간보고 26주차 |
| 2026-06-10 | opt only score — GS 47, RB 9 → **106/110 = 96.36%** | 커밋 `fecda8b` |
| 2026-06-23 | full stream **96.35** (opt 99, explorer 95.5, modeler 100) | 커밋 `4deff8f` |
| 2026-07-08 | 중간보고 — KPI 6개 항목 전부 PASS | 중간보고 |
| 2026-07~08 | 고차원 정체 해소, 경계 복원 수정, bootstrap 병렬화 수리 | 커밋 `b36caaa`, `3f3a0ea`, `6dc4eb9` |

> 위 수치는 문서·커밋 메시지에 기록된 값이다. `scaling` 브랜치(`2edbc7e`, 2026-08-28) 기준 재측정 결과는 아직 없다. 7~8월 변경이 반영된 점수는 배치를 다시 돌려야 나온다.

---

## 14. 출처

**구글 드라이브**

- [[보고]iPIDO_중간보고_20260706.pptx](https://drive.google.com/file/d/1oAOZlZQgB4RZxNPfIiLZAYwZNE-f3WR0/view) — 국책 KPI 표 (2026-07-08)
- [Y26_주간보고서_IIT_분석.pptx](https://drive.google.com/file/d/1oelbHNv5d28-xFpjPhx76drDHXIf9G7s/view) — 주차별 진척·점수 이력

**저장소 (`scaling` @ `2edbc7e`)**

- `CLAUDE.md` — 지표/목표 요약, 결과 분석 규약
- `docs/goals/optimizer_experiment_goals.md` — known optimum, goal, runtime 단축 정책
- `docs/playbook/full_pipeline_focus3_optimization_playbook.md` — 곱셈 게이트, 분석 순서, 실패 분류
- `CAE_model/cantilever_beam.py` — 목적함수·제약 정의
- `pipeline/run_pipelines.py` — ProblemCase 정의(L36–143), `_is_optimum_included` strict mode(L788–813), volume cap·bounds pass(L990–1003), goal hit(L1165–1179), summary 집계(L1440–1580)

**수치 검증** — 2026-08-30, `scipy.optimize.SLSQP` 400회 multi-start로 Cantilever Beam 제약 최적해 재계산 (92.7693, 응력 제약 binding)
